### McKinley & Rice React.js Task

Hosted URL: https://mckinley.netlify.com/home

## How to run locally

- Clone the project locally
- Use `npm install` to install the node_modules
- Use `npm start` to start the local server
